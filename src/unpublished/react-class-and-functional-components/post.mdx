---
title: React Component with class or functional (working on it)
slug: react-class-and-functional-components
image: ./images/person-holding-a-bible-1771217.jpg
date: "2020-04-23"
published: yes
tags: [js, nodejs, react]
---

import { CodeDivWrapper } from "../../css/commonstyles";
import Highlight from "react-highlight.js";
import { Link } from "gatsby";

React has 2 ways for creating components. I see this everytime and get puzzled why they look so different. At first I felt like there is a difference in applications because there are some functions that the class provides that I dont have in component

# Class way of definiing react Component

<CodeDivWrapper>

<Highlight>

```jsx
export default class CountryReults extends React.Component<Props, State> {
  constructor(props: Props) {
    super(props);
    this.handleShuffle = this.handleShuffle.bind(this);
    this.state = { currentName: "" };
  }

  handleShuffle() {
    const index = getRandomInt();
    const name = database[this.props.country].names[index];
    this.setState({ currentName: name });
  }

  render() {
    return (
      <div>
        <CustomButton
          onClick={this.handleShuffle}
          title={this.state.currentName}
        />
      </div>
    );
  }
}
```

</Highlight>

</CodeDivWrapper>

# The fucntional approach for this

<CodeDivWrapper>

<Highlight>

```jsx
const CountryReults = (props: Props) => {
  const [currentName, setCurrentName] = useState("");

  function handleShuffle() {
    const index = getRandomInt();
    const name = database[this.props.country].names[index];
    setCurrentName(name);
  }

  return (
    <div>
      <CustomButton
        onClick={this.handleShuffle}
        title={this.state.currentName}
      />
    </div>
  );
};
```

</Highlight>

</CodeDivWrapper>

# Application difference

- Functional way is just the more modern way.

# Source:

1. For gatsby: https://www.gatsbyjs.org/blog/2019-01-31-using-react-context-api-with-gatsby/
